# Keymaps

`ergonaut_one.svg` is generated from the live ZMK keymap with `just draw`.
The rendering configuration and the Ergonaut physical-layout input live in
`draw/`; generated diagrams belong here so they can be reviewed alongside the
firmware configuration.
