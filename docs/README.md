# Documentation

`ergonaut_one.svg` is generated from the live ZMK keymap with `just draw`.
It also holds the drawing configuration, Ergonaut physical-layout input, and
the small Nix package definition used to install keymap-drawer.
